node SineOscillator {
  <- out audio
  -> frequency control = 440 @smooth(10ms)

  phase = 0

  @process {
    out = sin(phase * TAU)
    phase += frequency / sampleRate
    if (phase >= 1) {
      phase -= 1
    }
  }
}
